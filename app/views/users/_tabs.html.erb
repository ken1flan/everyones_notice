<%
    nav_links = [
      [@user.nickname, user_path(@user)],
      ["きづき", notices_user_path(@user)],
      ["返信", replies_user_path(@user)],
      ["アクティビティ", activities_user_path(@user)]
    ]
%>

<%= render "shared/tabs", links: nav_links %>
