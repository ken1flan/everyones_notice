<%
    action_name = controller.action_name
    nav_links = [
      ["ユーザ一覧", "/", :active],
    ]
%>

<%= render "shared/tabs", links: nav_links %>
<br>

<% @users.each do |user| %>
  <%= render "detail", user: user %>
<% end %>
